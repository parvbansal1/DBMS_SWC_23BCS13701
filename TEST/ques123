-- CREATE TABLE transactions
-- (
--  transaction_id INT PRIMARY KEY,
--  merchant_id INT,
--  credit_card_id INT,
--  amount INT,
--  transaction_timestamp TIMESTAMP
-- );
-- INSERT INTO transactions
-- VALUES
-- (1, 101, 1, 100, '2022-09-25 12:00:00'),
-- (2, 101, 1, 100, '2022-09-25 12:08:00'),
-- (3, 101, 1, 100, '2022-09-25 12:28:00'),
-- (4, 102, 2, 300, '2022-09-25 12:00:00'),
-- (6, 102, 2, 400, '2022-09-25 14:00:00');

-- select transaction_id 
-- from transactions t1
-- join transactions t2
-- on t1.transaction_id = t2.transaction_id
-- where t1.merchant_id = t2.merchant_id
-- and t1.credit_card_id = t2.credit_card_id
-- and t1.amount = t2.amount;




-- CREATE TABLE user_actions
-- (
--  user_id INT,
--  event_id INT,
--  event_type VARCHAR(20),
--  event_date TIMESTAMP
-- );
-- INSERT INTO user_actions
-- VALUES
-- (445, 7765, 'sign-in', '2022-06-05 12:00:00'),
-- (742, 6458, 'sign-in', '2022-06-10 12:00:00'),
-- (648, 3124, 'like', '2022-06-18 12:00:00'),
-- (445, 3634, 'like', '2022-07-05 12:00:00'),
-- (742, 1374, 'comment', '2022-07-15 12:00:00'),
-- (999, 5555, 'sign-in', '2022-07-20 12:00:00');

-- select event_date as month , 
-- count(user_id) as month_active_users
-- from user_actions
-- where event_type in ('sign-in','like','comment')
-- and month(event_date) in ('july' , 'june')
-- and year(event_date) = 2022
-- group by user_id;


-- CREATE TABLE Submissions (
--  sub_id INT,
--  parent_id INT
-- );
-- INSERT INTO Submissions (sub_id, parent_id) VALUES
-- (1, NULL),
-- (2, NULL),
-- (1, NULL),
-- (12, NULL),
-- (3, 1),
-- (5, 2),
-- (3, 1),
-- (4, 1),
-- (9, 1),
-- (10, 2),
-- (6, 7);


select  (
select sub_id from Submissions where parent_id is null
)as post__id , 
 count(sub_id)as no_of_comments
from Submissions
group by parent_id;


select s1.sub_id as post_id
-- count(s2.parent_id) as no_of_comments
from Submissions s1 
left join Submissions s2
on s1.parent_id = s2.sub_id
where s1.parent_id is null
group by s2.parent_id;







